model_options:
  test_size: 0.2
  random_state: 0
  label:
    label
  features: [
    'total_credit_card_limit', 
    'current_available_limit', 
    'amount',
    'total_minus_current', 
    'current_available_limit_ratio',
    'amount_ratio_from_total', 
    'amount_ratio_from_current', 'is_F', 'is_M',
    'is_the_same_city', 'is_the_state', 'is_the_state_conf'
  ]